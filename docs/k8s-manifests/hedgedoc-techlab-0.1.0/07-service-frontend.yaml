apiVersion: v1
kind: Service
metadata:
  name: vibe-hedgedoc-frontend
  namespace: techlab
  labels:
    app.kubernetes.io/name: vibe-hedgedoc-frontend
    app.kubernetes.io/part-of: hedgedoc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vibe-hedgedoc-frontend
  ports:
    - name: http
      port: 3001
      targetPort: http
