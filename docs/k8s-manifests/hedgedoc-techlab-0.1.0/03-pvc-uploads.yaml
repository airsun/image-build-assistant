apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vibe-hedgedoc-uploads
  namespace: techlab
  labels:
    app.kubernetes.io/part-of: hedgedoc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: ""  # 需要时取消注释并填写集群 StorageClass
